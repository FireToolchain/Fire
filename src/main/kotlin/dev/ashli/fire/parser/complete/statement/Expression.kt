package dev.ashli.fire.parser.complete.statement

/**
 * Represents a statement that is just a single expression.
 * (This should generate a warning if the expression is Pure)
 */
class Expression {
}